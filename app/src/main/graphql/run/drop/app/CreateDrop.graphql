mutation CreateDrop($message: String!, $latitude: Float!, $longitude: Float!, $altitude: Float!) {
  createDrop(
    text: $message
    location: { latitude: $latitude, longitude: $longitude, altitude: $altitude}
  ) {
    id
  }
}